anthropic==0.111.0
holidays==0.99
numpy==2.5.0
openai==2.43.0
pdfplumber==0.11.10
psycopg-binary==3.3.4
psycopg==3.3.4
pytest==9.1.1
python-dotenv==1.2.2
requests==2.34.2
rich==15.0.0
scikit-learn==1.9.0
scipy==1.18.0
statsmodels==0.14.6
typer==0.26.7

# --- Capa RAG ---
# sqlite-vec: k-NN sobre la SQLite que el proyecto ya usa. Opcional: el backend
#   por defecto (NumpyIndex) es exacto y no necesita nada.
# model2vec: embebedor LOCAL sin claves. Inferencia en numpy puro — a propósito
#   NO se usa sentence-transformers, que arrastra torch (~2,5 GB) para un
#   pipeline cuyas demás dependencias suman una fracción de eso.
# Los tests no necesitan ninguno de los dos: usan FakeEmbedder.
sqlite-vec==0.1.9
model2vec==0.8.2
